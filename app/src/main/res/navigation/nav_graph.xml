<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentMain">

    <fragment
        android:id="@+id/fragmentMain"
        android:name="com.example.plantillaproyectoandroidkotlin.fragments.FragmentMain"
        android:label="fragment_main"
        tools:layout="@layout/fragment_main" >
        <action
            android:id="@+id/action_fragmentMain_to_fragmentNext"
            app:destination="@id/fragmentNext" />
    </fragment>
    <fragment
        android:id="@+id/fragmentNext"
        android:name="com.example.plantillaproyectoandroidkotlin.fragments.FragmentNext"
        android:label="fragment_next"
        tools:layout="@layout/fragment_next" >
        <action
            android:id="@+id/action_fragmentNext_to_fragmentMain"
            app:destination="@id/fragmentMain" />
    </fragment>
</navigation>